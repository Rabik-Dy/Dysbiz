<odoo>
    <record id="email_template_to_first_approver" model="mail.template">
        <field name="name">Time Off Request to First Approver</field>
        <field name="model_id" ref="hr_holidays.model_hr_leave"/>
        <field name="email_from">${(user.email_formatted or 'noreply@example.com')|safe}</field>
        <field name="email_to">${object.employee_id.parent_id.work_email|safe}</field>
        <field name="subject">Time Off Request from ${object.employee_id.name}</field>
        <field name="body_html"><![CDATA[
            <p>Dear ${object.employee_id.parent_id.name},</p>
            <p>${object.employee_id.name} has requested time off from ${object.request_date_from} to ${object.request_date_to}.</p>
            <p>Please review and approve the request.</p>
        ]]></field>
    </record>

    <record id="email_template_to_second_approver" model="mail.template">
        <field name="name">Time Off Request to Second Approver</field>
        <field name="model_id" ref="hr_holidays.model_hr_leave"/>
        <field name="email_from">${(user.email_formatted or 'noreply@example.com')|safe}</field>
        <field name="email_to">${object.second_approver_id.work_email|safe}</field>
        <field name="subject">Time Off Request Approved by First Approver - Needs Your Approval</field>
        <field name="body_html"><![CDATA[
            <p>Dear ${object.second_approver_id.name},</p>
            <p>The time off request of ${object.employee_id.name} has been approved by the first approver.</p>
            <p>Please review and approve it.</p>
        ]]></field>
    </record>

    <record id="email_template_to_employee" model="mail.template">
        <field name="name">Time Off Approved Notification</field>
        <field name="model_id" ref="hr_holidays.model_hr_leave"/>
        <field name="email_from">${(user.email_formatted or 'noreply@example.com')|safe}</field>
        <field name="email_to">${object.employee_id.work_email|safe}</field>
        <field name="subject">Your Time Off Request Has Been Approved</field>
        <field name="body_html"><![CDATA[
            <p>Dear ${object.employee_id.name},</p>
            <p>Your time off request from ${object.request_date_from} to ${object.request_date_to} has been fully approved.</p>
            <p>Enjoy your time off!</p>
        ]]></field>
    </record>
</odoo>
